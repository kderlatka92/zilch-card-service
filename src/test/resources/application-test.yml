spring:
  datasource:
    url: jdbc:tc:postgresql:17:///test_card_service
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: WARN
    kderlatka: DEBUG